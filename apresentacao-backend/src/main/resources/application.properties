# ----------------------------------------------------
# 1. CONFIGURA��O DE BANCO DE DADOS EM MEM�RIA (H2)
# ----------------------------------------------------

# O H2 � o banco embutido mais r�pido, ideal para testes de integra��o e desenvolvimento local.
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:medflowdb;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=password

# ----------------------------------------------------
# 2. CONFIGURA��O JPA/HIBERNATE
# ----------------------------------------------------

# Informa ao Hibernate que ele est� rodando no H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# CRUCIAL: Diz ao Hibernate para N�O criar as tabelas. 
# Quem far� isso � o Flyway, que usar� seus scripts V3__CATALOGO.sql.
spring.jpa.hibernate.ddl-auto=none

# ----------------------------------------------------
# 3. CONFIGURA��O FLYWAY
# ----------------------------------------------------

# Garante que o Flyway ser� executado na inicializa��o, rodando suas migra��es.
spring.flyway.enabled=true