VITE_API_BASE_URL=http://localhost:8080

# Se a sua API estiver servindo endpoints em um contexto (ex.: /backend),
# mantenha apenas a origem aqui (http://localhost:8080). As chamadas no
# frontend usam caminhos absolutos como /backend/tipos-exames.

# Para rodar o dev server na porta 8090 no Windows PowerShell:
# $env:PORT=8090
# npm run dev -- --port 8090
